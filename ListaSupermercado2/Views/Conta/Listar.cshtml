@model List<ListaSupermercado2.Models.Conta>
@{
    ViewBag.Title = "Listar";
}

<h2>Listar</h2>

<table>
    <thead>
        <tr>id</tr>
        <tr>usuario</tr>
        <tr>senha</tr>
    </thead>
    <tbody>
        @foreach (var c in Model)
        {
            <tr>
                <td>@c.ContaId</td>
                <td>@c.Usuario</td>
                <td>@c.Senha</td>
            </tr>    
        }
    </tbody>
</table>